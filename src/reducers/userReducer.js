const initialState = {
    user: null
  };
  
  const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // handle actions here
      default:
        return state;
    }
  };
  
  export default userReducer;
  